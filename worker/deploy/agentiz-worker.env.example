# Deprecated since profile-based onboarding. Do not place a worker token in an environment file.
#
# Run `agentiz-worker configure` as the service user instead. It writes the endpoint, token,
# stable instance ID, workspace and optional OpenHands image to
# ~/.config/agentiz/worker.json with mode 0600. The systemd unit reads that profile directly.
