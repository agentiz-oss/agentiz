/**
 * Configuration guides for the node types app-agentiz contributes to the workflow palette.
 *
 * They live with the definitions rather than in `docs/panel/`, because that is the contract of
 * `NodeTypeDefinition.docs`: the module that registers a node registers its documentation with it,
 * and `AppWorkflow` collects the bodies of every registered type into one documentation source
 * (`workflow.<type>` in the viewer, the panel search and the assistant's documentation skills) and
 * hands the same text to the canvas and to `agentiz.workflowSchema` over MCP.
 *
 * Two things are deliberately **not** written here: the node's one-line caption (that is
 * `description` on the definition, shown in the palette) and the list of config fields — the
 * article generates that from the same `configSchema` the editor renders a form from, so it cannot
 * drift. What belongs here is what a schema cannot say: when to use the node, what its outputs
 * mean, and the traps.
 *
 * Headings start at `##`: the `#` of the article is generated from the title.
 */

import type { NodeDocumentation } from '@nodeknit/app-workflow';

export const taskTriggerDocs: NodeDocumentation = {
  title: 'Триггер: задача Agentiz',
  keywords: ['триггер', 'задача', 'событие', 'agentiz.task.created', 'agentiz.task.updated'],
  locale: 'ru',
  body: `
## Когда срабатывает

Нода слушает факты о задачах, которые Agentiz публикует сам:

- **\`agentiz.task.created\`** — задача появилась: её создали в панели, принесла синхронизация
  трекера, добавил мобильный клиент или MCP.
- **\`agentiz.task.updated\`** — у задачи изменились **название, описание или теги**. Только они:
  статус задачи двигается во время каждого запуска, и флоу, который запускает пайплайны, иначе
  кормил бы сам себя.
- **\`agentiz.task.commented\`** — в треде задачи появился комментарий: человека, агента или
  подтянутый из внешнего трекера (замечание можно написать прямо в GitLab/GitHub).

## Второй вход: как замыкается круг доработки

Ребра «назад» в графе нет и не будет — граф ацикличен. Круг замыкается **фактом предметной
области**: последняя нода ветки пишет замечания в тред (\`agentiz.task.comment\`), а второй
триггер на это реагирует и начинает новый запуск флоу. То есть у графа два входа — «задача
пришла» и «в задаче написали комментарий» — и обе ветки сходятся в одну общую цепочку.

Чтобы круг замыкался **сам**, у входа-комментария нужно вписать в \`authorKind\` и \`agent\`
(например \`human, agent\`): по умолчанию там только \`human\`, то есть флоу ждёт живого
человека.

## Предохранители второго входа

Два из них — **жёсткие правила**, их нельзя выключить настройкой:

1. Комментарий с заполненным \`runId\` не поднимает триггер **никогда**. Итог каждого запуска
   уезжает в тред именно так, и реагировать на него значит реагировать на собственный вывод.
2. Служебная пометка флоу (\`meta.silent\`, её пишет \`agentiz.task.status\`) тоже не поднимает
   ничего.

Остальные — настройки, и у события «прокомментирована» у них свои значения по умолчанию, потому
что до появления этого события ни один сохранённый граф на них опираться не мог:

- **\`authorKind\`** — чьи комментарии считать. По умолчанию \`human\`, пусто = любые.
- **\`maxRounds\`** — сколько запусков этого флоу по одной задаче допускать. По умолчанию **3**
  для события «прокомментирована» и **0 (без ограничения)** для остальных. Круги считаются по
  таблице (\`AgentWorkflowRun\` с этим \`taskId\` и \`specId\`), поэтому число видно человеку.
  Исчерпан — флоу не стартует, а в карточке задачи появляется статус «Круги доработки исчерпаны».
- **\`skipIfFlowActive\`** — не стартовать поверх флоу, который уже управляет этой задачей. По
  умолчанию включено для «прокомментирована» и выключено для остальных (так вели себя все графы,
  написанные раньше). Человек может дописать три комментария подряд — запуск будет один.

Триггер — единственный способ начать флоу. «Запустить воркфлоу» как действия не существует;
ручной жест в канвасе и в MCP (\`agentiz.fireWorkflowTrigger\`) называет **конкретную** триггер-ноду,
потому что у графа их может быть несколько.

## Как ограничить проектом

Поле \`projectId\` — это фильтр событий, и именно оно решает, задачи какого проекта попадут во флоу.
Привязка самого воркфлоу к проекту (её видно в списке и в редакторе) — **отдельная вещь**: она
описательная, нужна для раздела «Воркфлоу» в проекте и для фильтра в общем списке, и на то, какие
события дойдут до ноды, не влияет. Хочешь флоу одного проекта — заполни оба.

Пустой \`projectId\` = задачи всех проектов.

## Что уезжает дальше по флоу

\`msg.payload\` — плоская задача: \`taskId\`, \`projectId\`, \`title\`, \`description\`, \`tags\`,
\`status\`, \`priority\`, \`externalId\`, \`externalUrl\`, \`sourceType\`. У \`agentiz.task.updated\`
добавляется \`changed\` — какие из отслеживаемых полей тронул этот save. У
\`agentiz.task.commented\` добавляются \`commentId\`, \`body\`, \`authorKind\`, \`authorName\`,
\`origin\`, \`runId\` и \`silent\`; \`commentId\` — не украшение, по нему «Пайплайн (с ожиданием)»
отдаёт замечание агенту как текущее задание.

«Проверка задачи» и «Запустить пайплайн (без ожидания)» дополняют этот же объект. «Пайплайн (с
ожиданием)» — единственная, кто payload **заменяет**: после неё в нём лежит результат запуска.
`.trim(),
};

export const taskMatchDocs: NodeDocumentation = {
  title: 'Проверка задачи',
  keywords: ['фильтр', 'условие', 'ключевые слова', 'теги', 'ветвление'],
  locale: 'ru',
  body: `
## Зачем

Решает, стоит ли задача запуска, и разводит флоу на два выхода: **\`match\`** (подходит) и
**\`no\`** (мимо). Ветку \`no\` можно вообще никуда не вести — флоу просто закончится.

## Как задаются условия

- **Слова** ищутся подстрокой, регистр не важен: \`выполни\` найдётся и в «Выполни рефакторинг», и
  в «нужно выполнить» — это цена подстроки, не ошибка. Несколько слов — через запятую, срабатывает
  любое из них.
- **Теги** сравниваются целиком (регистр не важен): \`todo\` не совпадёт с \`todo-later\`.
- **\`fields\`** сужает поиск слов до названия или до описания; по умолчанию ищутся оба.
- **\`require\`**: \`any\` — достаточно слова **или** тега; \`all\` — нужно и слово, и тег.

## Ловушка

Пустое условие не подходит **никому**. Нода со снятыми словами и тегами не пропустит ни одной
задачи и уведёт всё в \`no\` — это сделано нарочно: молча запускать пайплайн на каждую входящую
задачу хуже, чем не запускать ничего.

## Что уезжает дальше

К задаче добавляется \`match: { matched, keywords, tags }\` — что именно совпало. Удобно, когда
дальше по ветке нужно понять, по какому слову задача прошла.
`.trim(),
};

export const taskRunDocs: NodeDocumentation = {
  title: 'Запустить пайплайн (без ожидания)',
  keywords: ['пайплайн', 'запуск', 'run', 'без ожидания'],
  locale: 'ru',
  body: `
## Чем отличается от «Пайплайн (с ожиданием)»

Эта нода ставит задачу в пайплайн и **идёт дальше сразу**, не дожидаясь результата: следующая нода
получит управление, пока агент ещё работает. Нужен результат запуска — берите
\`agentiz.pipeline\`. Это два разных типа нод, а не флажок: движок различает их по виду
(\`server\` против \`external\`), а вид не может зависеть от настройки.

## Какой пайплайн запустится

Нода не выбирает спеку — за неё это делает Agentiz по обычным правилам: активные спеки проекта
задачи, приоритет у той, чьи \`matchTags\` совпали с тегами задачи, иначе дефолтная. Поэтому у
проекта должна быть **хотя бы одна активная спека**, иначе запуск упадёт с
\`No active pipeline spec for project …\`.

## Настройки

- **\`skipIfActive\`** (по умолчанию включено) — не запускать, если задача уже в работе
  (\`queued\`, \`running\`, \`waiting_input\`). Задача перечитывается из базы прямо здесь: событие
  было заморожено в момент записи задачи, а запуск мог начаться между тем и этим моментом.
- **\`trigger\`** — чем пометить запуск в \`AgentRun.trigger\`. Своего значения \`workflow\` в ENUM
  пока нет, поэтому по умолчанию \`sync\`.

## Что уезжает дальше

\`runId\` — id созданного запуска, либо \`null\` и \`skipped: "task_<статус>"\`, если запуск
пропущен из-за \`skipIfActive\`.
`.trim(),
};

export const pipelineDocs: NodeDocumentation = {
  title: 'Пайплайн (с ожиданием)',
  keywords: ['пайплайн', 'ожидание', 'external', 'успех', 'ошибка', 'вердикт', 'pass', 'fail'],
  locale: 'ru',
  body: `
## Что делает

Запускает пайплайн по задаче и **паркует флоу**, пока запуск не завершится — обычно это минуты или
часы. Когда запуск закончился, флоу продолжается с одного из четырёх выходов:

- **\`succeeded\`** — запуск завершился успешно, и пайплайн **не спрашивал** машинный вердикт (ни
  у одной стадии нет \`verdict: true\`);
- **\`failed\`** — запуск не смог выполниться (инфраструктурная поломка, агент не доехал до
  результата). Это **результат**, а не сломанный флоу: граф идёт дальше по своей ветке, и текст
  ошибки лежит в \`msg.payload.error\`;
- **\`pass\`** / **\`fail\`** — запуск завершился успешно, и пайплайн спрашивал вердикт: сюда
  уходит **мнение агента** («тесты прошли» / «тесты не прошли»), а не инфраструктурный статус.
  \`failed\` при этом не срабатывает — агент выполнился, просто с отрицательным выводом.

**Важно при включении вердикта на уже подключённом пайплайне:** ребро \`succeeded\` перестаёт
срабатывать вообще — граф нужно перецепить на \`pass\`/\`fail\`, иначе эта ветка просто замолчит.
Подробности о том, как стадия просит вердикт — \`stages[].verdict\` в схеме пайплайна
(\`agentiz.pipelineSpecSchema\`).

## Почему это переживает деплой

Ожидание durable: флоу лежит в базе (\`AgentWorkflowRun\`, статус \`waiting_external\`, ссылка
\`run:<id>\`), а не в памяти процесса. Перезапуск сервера во время работы агента ничего не теряет —
завершившийся запуск находит ждущий флоу по этой ссылке.

## Выбор пайплайна и настройки

- **\`specId\`** — запустить **именно эту** спеку. Пусто = как раньше: активные спеки проекта
  задачи, приоритет по \`matchTags\`, иначе дефолтная. Для графа «сначала разработчик, потом
  тестировщик» это обязательное поле: по тегам две спеки одной задачи не различить, обе
  зарезолвятся в одну. Спека проверяется на принадлежность проекту задачи — чужая отвергается,
  а не подменяется молча.
- **\`useTriggerComment\`** (по умолчанию включено) — если флоу начался с комментария, отдать
  этот комментарий агенту как **текущее задание**, а не как одну из строк треда. Без этого круг
  доработки работает формально: агент читает запуск как «сделай задачу заново».
- **\`trigger\`** — метка запуска в \`AgentRun.trigger\`.

В отличие от \`agentiz.task.run\`, проверки «задача уже в работе» здесь нет: нода, которая ждёт
конкретный запуск, должна этот запуск создать.

## Что уезжает дальше

Payload **заменяется** результатом запуска: \`runId\`, \`taskId\`, \`projectId\`, \`status\`,
\`summary\`, \`error\` (последнее — на ветке \`failed\`) и \`verdict\` (\`"pass"\` / \`"fail"\` /
\`null\`). Полей задачи после этой ноды в payload уже нет: нужны они дальше — читайте задачу по
\`taskId\`.
`.trim(),
};


export const taskStatusDocs: NodeDocumentation = {
  title: 'Статус задачи (текст)',
  keywords: ['статус', 'карточка', 'ждём человека', 'workflowStatus'],
  locale: 'ru',
  body: `
## Зачем

Пишет в задачу одну строку — где работа стоит **на языке заказчика**: «в разработке», «на
проверке», «ждём тестировщика», «в релизе». Её видно в карточке задачи в панели, в мобильном
\`GET /tasks/:id\` и в MCP.

Это **не** \`AgentTask.status\`. Тот ENUM принадлежит пайплайну (\`queued\`, \`running\`,
\`done\`, …), и трогать его нельзя: он двигается сам во время каждого запуска. Текстовый статус
живёт в отдельном поле \`workflowStatus\` и говорит то, для чего у пайплайна состояния нет.

## Почему это не разбудит собственный флоу

\`workflowStatus\` намеренно **не** входит в список полей, за которыми следит триггер
\`agentiz.task.updated\` (там только название, описание и теги). Флоу, пишущий свой статус, не
кормит сам себя.

То же и с \`alsoComment\`: пометка в треде помечается служебной (\`meta.silent\`), и вход
«в задаче написали комментарий» её игнорирует по жёсткому правилу.

## Подстановки

\`{{payload.<поле>}}\` — любое поле текущего \`msg.payload\`: \`{{payload.title}}\`,
\`{{payload.verdict}}\`, \`{{payload.summary}}\`, \`{{payload.comment}}\`. Неизвестный путь
подставляется пустой строкой. Ничего не вычисляется — только подстановка текста.
`.trim(),
};

export const taskCommentDocs: NodeDocumentation = {
  title: 'Комментарий в задачу',
  keywords: ['комментарий', 'замечания', 'круг', 'доработка', 'тред'],
  locale: 'ru',
  body: `
## Зачем

Пишет замечания в тред задачи — и это **конец круга** и одновременно его начало: комментарий
поднимает вход «в задаче написали комментарий», и по задаче идёт новый запуск флоу. Ребра назад в
графе нет, круг замыкается фактом в предметной области.

Сюда сходятся обе «плохие» ветки: \`fail\` от агента-тестировщика и «отклонено» от человека.
Ноду можно (и нужно) использовать одну на обе — движок разрешает fan-in, две ветки в одном
запуске никогда не живут одновременно.

## Настройки, о которых стоит знать

- **\`releasesTask\`** (по умолчанию включено) — снимает с задачи признак «ею управляет этот
  флоу» **до** записи комментария. Порядок здесь важен: иначе следующий круг упрётся в
  предохранитель \`skipIfFlowActive\` и не начнётся вовсе. Выключайте только если после этой
  ноды граф ещё что-то делает.
- **\`authorKind\`** — от чьего имени. По умолчанию \`agent\`. Чтобы такой комментарий поднимал
  вход-комментарий, у триггера в \`authorKind\` должен быть вписан \`agent\`.

Комментарий пишется **без** \`runId\` — специально: комментарий с \`runId\` считается отчётом
запуска и не поднимает триггер никогда.

## Подстановки

Те же, что у статуса. Полезные после «Пайплайна с ожиданием»: \`{{payload.summary}}\`,
\`{{payload.verdict}}\`, \`{{payload.error}}\`; после «Решения человека» — \`{{payload.comment}}\`
(текст отказа, который человек написал).
`.trim(),
};

export const approvalDocs: NodeDocumentation = {
  title: 'Решение человека',
  keywords: ['приёмка', 'человек', 'заявка', 'approval', 'принять', 'отклонить'],
  locale: 'ru',
  body: `
## Что делает

Создаёт **заявку на решение** и паркует флоу, пока человек не нажмёт «принять» или «отклонить».
Два выхода: \`approved\` и \`rejected\`. Отказ — это **результат**, а не поломка: граф идёт по
своей ветке, а текст причины лежит в \`msg.payload.comment\`.

## Чем это отличается от вопроса агента

Вопрос агента (\`AgentRunInteraction\`) держит живой запуск: агент буквально стоит посреди хода и
занимает воркер. Заявка не держит **ничего** — ни воркера, ни папку; она переживает запуск,
рестарт и деплой и закрывается только решением человека либо отменой флоу. Поэтому это отдельная
таблица, а не вопрос агента.

## Кому она достанется

Адресуется **токеном**, а не ролью и не группой: по умолчанию \`agentiz-approval-decide\`, который
несут «Тестировщики» и все ступени выше. Так новая роль, получившая этот токен, начинает получать
заявки без правки графа. Владелец проекта получает их всегда.

\`assigneeUserId\` сужает до одного человека («эту фичу смотрит Пётр») и ничего не расширяет:
право всё равно проверяется по токену.

Уведомление уходит **только** тем, кто вправе решать. Остальные увидят событие в ленте — она
пишется всегда, независимо от политики доставки.

## Что видит человек

Заголовок, текст и ссылки (\`Название|https://…\`, по одной в строке) — именно по ссылкам он и
проверяет. Заявка приходит пушем, лежит во «Входящих» мобильного клиента как **блокирующая**
строка (её нельзя «скрыть»: она держит флоу) и открывается в карточке задачи в панели.

**Отказ без текста не принимается**: этот текст получает агент как следующее задание, и «отклонено»
без причины отправляет его переделывать задачу с нуля.

## Ловушки

- Отмена флоу закрывает его незакрытые заявки (\`cancelled\`) — иначе строка во «Входящих»
  пережила бы воркфлоу, которое её ждало.
- Повторная отправка той же ноды не плодит заявок: на пару «запуск флоу + нода» открытая заявка
  всегда одна.
`.trim(),
};
